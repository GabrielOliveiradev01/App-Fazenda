# Site estático (HTML/CSS/JS) — sem build de framework
[build]
  command = "true"
  publish = "."

# Garante que index.html seja a home
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
